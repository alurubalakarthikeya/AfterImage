import {
  AlertTriangle,
  Archive,
  ArrowDown,
  ArrowDownUp,
  ArrowRight,
  ArrowUp,
  ArrowUpRight,
  Bell,
  BellOff,
  Braces,
  Briefcase,
  Bug,
  Calendar,
  CalendarDays,
  CalendarRange,
  Check,
  CheckCircle2,
  ChevronDown,
  AppWindow,
  ChevronLeft,
  ChevronRight,
  Clipboard,
  Clock,
  Command,
  Copy,
  CornerDownLeft,
  Cpu,
  Database,
  Download,
  ExternalLink,
  Eye,
  File,
  FileText,
  Files,
  Film,
  Filter,
  Folder,
  FolderKanban,
  FolderOpen,
  FolderPlus,
  Gauge,
  GraduationCap,
  HardDrive,
  Hash,
  Home,
  Image as ImageIcon,
  Info,
  Keyboard,
  Layers,
  LayoutGrid,
  List,
  Loader2,
  Lock,
  Maximize2,
  Minus,
  Monitor,
  MonitorSmartphone,
  Moon,
  MoreHorizontal,
  Music,
  Palette,
  PanelLeft,
  PanelRight,
  Pause,
  Pencil,
  Pin,
  Play,
  Plus,
  RefreshCw,
  Rows3,
  ScanText,
  Search,
  Server,
  Settings,
  Shield,
  SlidersHorizontal,
  Smile,
  Square,
  Star,
  Sun,
  Tag,
  Terminal,
  Trash2,
  Type,
  Upload,
  User,
  X,
  Zap,
  type LucideProps,
} from 'lucide-react';

/**
 * Icon registry.
 *
 * Collections, context menus and settings rows store icon *names* (they are
 * data). This map is the only place names become components, so dropping one
 * in later is a one-line change and nothing else has to import lucide.
 */
const REGISTRY = {
  AlertTriangle,
  Archive,
  ArrowDown,
  ArrowDownUp,
  ArrowRight,
  ArrowUp,
  ArrowUpRight,
  Bell,
  BellOff,
  Braces,
  Briefcase,
  Bug,
  Calendar,
  CalendarDays,
  CalendarRange,
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Clipboard,
  Clock,
  Command,
  AppWindow,
  Copy,
  CornerDownLeft,
  Cpu,
  Database,
  Download,
  ExternalLink,
  Eye,
  File,
  FileText,
  Files,
  Film,
  Filter,
  Folder,
  FolderKanban,
  FolderOpen,
  FolderPlus,
  Gauge,
  GraduationCap,
  HardDrive,
  Hash,
  Home,
  Image: ImageIcon,
  Info,
  Keyboard,
  Layers,
  LayoutGrid,
  List,
  Loader2,
  Lock,
  Maximize2,
  Minus,
  Monitor,
  MonitorSmartphone,
  Moon,
  MoreHorizontal,
  Music,
  Palette,
  PanelLeft,
  PanelRight,
  Pause,
  Pencil,
  Pin,
  Play,
  Plus,
  RefreshCw,
  Rows3,
  ScanText,
  Search,
  Server,
  Settings,
  Shield,
  SlidersHorizontal,
  Smile,
  Square,
  Star,
  Sun,
  Tag,
  Terminal,
  Trash2,
  Type,
  Upload,
  User,
  X,
  Zap,
} satisfies Record<string, React.ComponentType<LucideProps>>;

export type IconName = keyof typeof REGISTRY;

export function iconNames(): IconName[] {
  return Object.keys(REGISTRY) as IconName[];
}

export interface IconProps extends Omit<LucideProps, 'ref'> {
  name: string;
}

/** Render a stored icon name; unknown names degrade to a generic file glyph. */
export function Icon({ name, ...props }: IconProps) {
  const Component = REGISTRY[name as IconName] ?? File;
  return <Component {...props} />;
}
